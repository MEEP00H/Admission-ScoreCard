import React from 'react';
import './shareBtn.css';

const shareBtn = (props) => {
    return   <img src = {props.shareImage} alt = 'shareImage' className = 'shareImage'/>
} 

export default shareBtn;